# LLM 配置（默认使用智谱 GLM-4-Flash，免费、国内直连）
# 申请免费 API Key：https://open.bigmodel.cn/
#   1. 注册登录
#   2. 进入「控制台」→「API Keys」→ 创建新的 API Key
#   3. 把 Key 填到下面 LLM_API_KEY 即可
#
# 也可以换成任何 OpenAI 兼容接口（DeepSeek / 通义千问 / Moonshot / OpenAI 等），
# 只需改下面三个变量。例如 DeepSeek：
#   LLM_BASE_URL=https://api.deepseek.com
#   LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_API_KEY=
LLM_MODEL=glm-4-flash
