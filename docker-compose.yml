version: "3.8"

services:
  meeting_parser:
    build: .
    image: meeting_parser:latest
    container_name: meeting_parser
    ports:
      - "8124:8124"
    volumes:
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
    restart: unless-stopped
    env_file:
      - .env
